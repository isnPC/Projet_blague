def ajouterBlague():
    """permet d'ajouter une blague à un fichier
       paramètre : nom fichier
       ptype : chaine de caractères
       sortie : la nouvelle blague dans le bon fichier
       stype : chaine de caractères"""
    
    maBlague = open("monTitre.txt","a")
    #les blagues sont numérotées selon le nombres de blagues dans le fichier
    maBlague.write("**"+str(compteBlague("monTitre.txt")+1))
    Blague = input("entrez votre blague:")
    maBlague.write(Blague)
    maBlague.close()

ajouterBlague()
